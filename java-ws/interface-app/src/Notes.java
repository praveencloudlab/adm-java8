public class Notes {
    /**
     *  tightly coupling : abstract class (partial)
     *  loosely coupling : interface (100%)
     *
     *
     * -> Interface supports extensibility
     * -> abstract class do not support extensibility
     *
     *
     *
     */
}
